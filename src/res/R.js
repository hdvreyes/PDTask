// This will serve as namespace to encapsulate our resources.
// We will access other resources using R.[resource]
import strings from "./strings";
import images from "./images";
import colors from "./colors";
import palette from "./palette";

const R = {
  strings,
  images,
  colors,
  palette
};

export default R;
